# 更新日志

## 5.6.2 — 品牌显示修复

- 修复恢复前端页面内仍显示“知瑶画布”的页头、欢迎引导和辅助文字，统一显示为“花海画布”。

## 5.6.1 — 更新发布可靠性修复

- 更新发布清单现在拒绝非 ASCII 资产名，防止 GitHub 上传工具截断中文文件名而导致自动更新下载失败。
- 完成 5.6.0 到 5.6.1 的内置更新端到端验证。

## 5.6.0 — 花海画布首次发布

- 将产品名称更新为“花海画布”，并使用新的应用与安装器图标。
- 新增内置更新入口：自动发现新版本，用户确认后下载、验证签名并安装。
- 更新包托管于 GitHub Releases，使用独立更新签名密钥验证，失败不会替换当前版本。
- Windows 安装器改为当前用户安装，通常不需要管理员权限。
- 保持原有程序标识与本地数据位置，已有项目、素材和设置不因品牌更新而迁移或删除。
# 5.7.0 — 模块化工作区

- 新增独立的花海画布侧栏、图标、项目主页入口及按功能拆分的 CSS/JS 模块。
- 新增本地 SQLite 对话会话、消息与“先预览再确认”的受限代理动作；API 密钥始终只留在本机且不随项目导出。
- 新增多图连线到生视频、横向/纵向排列、重复边跳过和独立撤销/重做历史。
- 新增验证后提交脚本：Rust/前端检查和暂存密钥扫描通过后才创建提交。
