# 花海画布 5.9.0

- Windows 安装和更新改用随应用放在 F 盘的固定 WebView2 运行时，不再下载或安装系统级 WebView2；项目数据、媒体、缓存和更新文件继续只使用 F 盘。
- 发布前会自动从 Microsoft 官方固定版运行时源准备运行时，并验证固定运行时配置；运行时文件和构建产物不会进入 Git。
- 包含 5.8.9 的画布拖图目标项目修复，以及 API 多分类模型合并修复。

> 固定运行时会使安装包体积增加，这是为了保证新电脑安装和自动更新不依赖 C 盘的系统级 WebView2。
