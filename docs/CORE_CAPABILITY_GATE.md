# 核心能力迁移门槛

恢复前端仍是花海画布的默认生产界面。新工作区只能作为预览代码存在，绝不能隐藏、覆盖或替换 `#root`。

以下每项必须在真实旧项目、断网错误和桌面打包环境中回归通过后，才可替换对应旧入口：项目管理与导入导出、画布节点和保存、图片与视频生成、素材库、视频工具、设置与自定义通道、登录与积分、更新检查。

任一预览模块初始化失败或功能回归时，应仅移除自身并继续保留旧界面。所有切换必须有独立的人工验收记录与自动测试。
