# 花海画布 5.8.6

- 将项目、聊天、设置、媒体、WebView 缓存和更新临时文件迁移到 `F:\Huahaihuabu\花海画布-data`，与程序安装目录隔离。
- 首次启动会在同一 F 盘原子迁移旧版数据目录；不会复制到 C 盘，也不会重建项目数据库。
- 若旧 WebView 缓存仍被 Edge 子进程锁定，应用会继续使用该缓存并在后续启动重试迁移，不会因此无法打开。
- 避免手动重装、自动更新或卸载程序文件时误伤用户项目和 API 设置。
