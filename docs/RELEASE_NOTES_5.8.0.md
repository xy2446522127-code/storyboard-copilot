# 花海画布 5.8.0

这是一次需要手动安装的安全过渡版本：更新签名密钥已轮换，安装后后续版本将恢复应用内自动更新。

- 全新花海画布独立工作区：项目管理、在线生图、GPT 对话、无限画布、素材库和 API 设置均不再依赖旧界面按钮。
- 所有项目、设置、聊天记录与 WebView 数据固定保存在 F 盘；安装器也固定安装至 `F:\Huahaihuabu\花海画布`。
- 新增安全的代理预览执行、批量图片连线与排列、撤销/重做、媒体懒加载和离屏视频暂停。
- 新公告源、花海画布图标与品牌名称已启用；旧公告源和退役服务流量已禁用。

安装后请在“API 设置”中填写自己的兼容 API 通道；密钥只保存在本机，不会上传或导出。
