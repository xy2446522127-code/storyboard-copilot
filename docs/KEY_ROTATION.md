# 花海画布 5.8 更新密钥轮换

5.8.0 使用新的更新签名密钥。旧版安装包内置的是旧公钥，无法自动验证新密钥签名的更新，因此必须由用户手动安装一次 5.8.0 过渡安装包。

新私钥和口令仅保存在本机的 `F:\Users\DXY\Desktop\CFWJ\花海画布-私钥\`，不属于仓库、不随安装包分发，也不能通过聊天、截图或 GitHub 上传。该目录只授予当前 Windows 用户访问。

5.8.0 安装完成后，后续版本继续使用同一把新私钥签名，应用内自动更新恢复正常。若私钥或口令遗失，必须再次轮换密钥并发布新的手动过渡安装包。
